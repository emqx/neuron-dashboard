import { AxiosResponse } from 'axios'
import http, { handleError } from '@/utils/http'

export const login = () => {
  // TODO:
  return Promise.resolve()
}

export const logout = () => {
  // TODO:
  return Promise.resolve()
}
